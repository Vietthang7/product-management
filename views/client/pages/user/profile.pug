extends ../../layouts/default.pug
include ../../mixins/box-head.pug

block main
  .container.my-3
    .row
      .col-12
        +box-head(pageTitle)

        div Họ và tên: #{user.fullName}
        div  Email : #{user.email}
      .col-2 
        a(href="/user/edit/:id" class="btn btn-dark btn-block") Chỉnh sửa
      .col-2 
        a(href="/user/password-old" class="btn btn-primary btn-block") Đổi mật khẩu
